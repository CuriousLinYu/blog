# SHAP 值在风控模型可解释性中的实战

风控模型最大的尴尬不是 AUC 不够高，而是审批被拒的用户打电话来问"凭什么拒我"，你只能回答"模型说的"。

信用评分模型通常用 XGBoost 或 LightGBM 这种"黑盒"，特征重要性排名可以看到哪些变量影响大，但看不到**某个具体用户**为什么被拒——是收入太低？是多头借贷太多？还是手机号仅注册 3 天？SHAP 值就是用来回答这个问题的。

> 这篇文章不会从头推导 Shapley 值的博弈论公式（那是教科书的事），而是聚焦在风控场景中怎么用、有什么坑、生产环境怎么部署。

---

## SHAP 值到底给出的是什么

一句话：**SHAP 值 = 把模型对某个样本的预测结果，拆解到每个特征身上，告诉你每个特征贡献了多少分。**

比如模型预测张三的违约概率是 0.35，SHAP 告诉你：

| 特征 | SHAP 值 | 含义 |
|---|---|---|
| 近6月查询次数=8 | +0.12 | 拉高了违约概率 12 个百分点 |
| 信用卡使用率=92% | +0.08 | 拉高了 8 个百分点 |
| 月收入=25000 | -0.06 | 拉低了 6 个百分点 |
| 在职年限=5年 | -0.04 | 拉低了 4 个百分点 |

所有 SHAP 值加在一起 = 0.12 + 0.08 - 0.06 - 0.04 = 0.10，加上 baseline（比如全体样本平均违约概率 0.25），正好等于模型输出 0.35。

**这个可加性是 SHAP 区别于其他特征归因方法（LIME、统一特征重要性）的核心优势。**

---

## 在风控场景怎么用

### 场景 1：拒因解释（最刚需）

被拒用户投诉到客服，运营把申请号给你，你需要 30 秒内给出拒因。

```python
import shap
import pandas as pd
import xgboost as xgb

# 模型和特征名
model = xgb.Booster()
model.load_model("credit_score_model.json")
feature_names = [
    "query_cnt_6m", "credit_card_util", "monthly_income",
    "tenure_year", "debt_ratio", "recent_default_cnt"
]

# 构造解释器
explainer = shap.TreeExplainer(model)

# 张三的特征向量
zhangsan = pd.DataFrame([{
    "query_cnt_6m": 8, "credit_card_util": 0.92,
    "monthly_income": 25000, "tenure_year": 5,
    "debt_ratio": 0.55, "recent_default_cnt": 0
}])

shap_values = explainer.shap_values(zhangsan)
base_value = explainer.expected_value

# 生成"人话"解释
contributions = list(zip(feature_names, shap_values[0]))
# 按 SHAP 绝对值排序，找出影响最大的 3 个
top_factors = sorted(contributions, key=lambda x: abs(x[1]), reverse=True)[:3]

for feature, value in top_factors:
    direction = "增加" if value > 0 else "降低"
    print(f"  {feature}: SHAP={value:.4f}，{direction}了违约概率")
```

### 场景 2：特征工程——找出反直觉的特征

SHAP 的 summary plot 能一眼看出哪些特征的取值方向反了。

```python
# 对验证集做全量 SHAP 分析
X_val = pd.read_csv("validation_features.csv")
shap_vals = explainer.shap_values(X_val)

# SHAP summary plot
shap.summary_plot(shap_vals, X_val, feature_names=feature_names)
```

风控中的一个常见发现：summary plot 里某个特征的高取值明明是正向的（比如"芝麻分=750"），SHAP 值却是负的（降低了违约概率？还是升高了？颜色和方向对不上）。这通常意味着：

1. **多重共线性**：这个特征和其他特征高度相关，模型把它的解释力分摊到了别的特征上，SHAP 只是如实反映了模型的分配方式
2. **特征加工有 bug**：取值方向在 ETL 中被反转了
3. **模型过拟合了某个噪声模式**：你的训练集里高芝麻分但违约的样本恰好占多数

### 场景 3：交互效应——"这两个特征在一起才出事"

单特征 SHAP 看完了，你还需要知道**特征组合的交互效应**。

```python
# SHAP interaction values
shap_interaction = explainer.shap_interaction_values(X_val.iloc[:1000])

# 画出 query_cnt_6m 和 credit_card_util 的交互热力图
shap.dependence_plot(
    ("query_cnt_6m", "credit_card_util"),
    shap_interaction, X_val.iloc[:1000],
    feature_names=feature_names
)
```

交互效应在风控中的典型发现：
- **查询次数多 + 信用卡使用率高** → 交互 SHAP 值远超各自独立贡献之和。单独多查询可能只是近期在比价贷款产品，单独信用卡用得多可能只是周转需求旺盛。两者叠加——"账户用到了快透支还在疯狂申贷"——才是真正的危险信号。
- **月收入低 + 债务比例高**的交互值比你想象的大。收入低本身不一定违约（消费习惯保守），债务高本身也不一定（高收入高负债的人很多），但两个负面特征叠在一起，意味着"已经入不敷出了"。

---

## 生产落地：SHAP 值怎么集成到风控系统

### 架构方案

```
请求进来 → 特征加工 → 模型推理 → SHAP 解释（异步）
                                    ↓
                              Kafka / 消息队列
                                    ↓
                         SHAP 值写入特征明细表
                                    ↓
                    审批系统查询 → "拒因可视化"
```

**关键设计决策：SHAP 解释和模型推理要分开。** 原因是 `TreeExplainer.shap_values()` 即使有 C 扩展优化，单样本也要 2-10ms（取决于特征数量和树深度），在 QPS 1000+ 的实时风控链路上不可接受。

```java
// Java 侧实时模型推理（不阻塞在 SHAP）
ScoreResult result = creditScoreService.score(request);
// result.score 直接用于策略决策

// SHAP 解释异步投递（不阻塞主链路）
if (result.score < threshold) {
    shapTaskProducer.send(new ShapTask(requestId, featureMap, result.score));
}
```

Python 消费端：

```python
import json
from kafka import KafkaConsumer

consumer = KafkaConsumer("shap_tasks", bootstrap_servers="kafka:9092")

for msg in consumer:
    task = json.loads(msg.value)
    features = task["features"]
    shap_vals = explainer.shap_values(pd.DataFrame([features]))

    # 找出 top 3 贡献
    top3 = sorted(
        enumerate(shap_vals[0]), key=lambda x: abs(x[1]), reverse=True
    )[:3]

    decline_reasons = []
    for idx, val in top3:
        direction = "偏高" if val > 0 else "偏低"
        decline_reasons.append({
            "feature": feature_names[idx],
            "feature_cn": feature_cn_map[feature_names[idx]],
            "impact": f"{val:+.4f}",
            "verdict": f"{direction}"
        })

    # 写入 MySQL / Redis，供审批系统查询
    save_shap_result(task["request_id"], decline_reasons)
```

### 两个踩过的坑

**坑 1：TreeExplainer 线程安全性**

`shap.TreeExplainer` 在 v0.41 之前不是线程安全的。如果你用 Gunicorn 多 worker 部署，每个 worker 在启动时实例化自己的 explainer，不要全局共享。

```python
# ✅ 每个 worker 自己持有
explainer = None

def init_worker():
    global explainer
    explainer = shap.TreeExplainer(model)

# ❌ 全局共享
explainer = shap.TreeExplainer(model)  # 多 worker 并发会有问题
```

**坑 2：baseline 要随客群调整**

`explainer.expected_value` 是训练集上的基值。但你的线上客群和训练集分布可能差异巨大——如果线上合格率 60%，训练集合格率 80%，baseline 的含义就变了。

建议做法：**每天用当天的样本重新计算 expected_value 并热更新。**

```python
# 定时任务：每小时采样最近 1000 条推理日志
def refresh_baseline():
    recent_samples = sample_recent_predictions(1000)
    new_baseline = explainer.expected_value  # 用新样本重新算
    redis.set("shap_baseline", json.dumps(new_baseline))
```

---

## SHAP 的局限：什么时候别用它

| 场景 | 是否适用 | 理由 |
|---|---|---|
| 解释 XGBoost/LightGBM 单样本 | ✅ 最佳 | TreeExplainer 计算快、结果精确 |
| 解释随机森林 100+ 棵树 | ✅ | 同样基于树结构，适用 |
| 解释深度神经网络 | ⚠️ 慢但可用 | 用 KernelExplainer 或 DeepExplainer，单样本解释可能上百毫秒 |
| 解释 500+ 特征的模型 | ⚠️ | 交互效应矩阵 O(n²)，需要特征降维后再做交互分析 |
| 在线实时决策（<5ms） | ❌ | 必须走异步 |
| 解释规则引擎/逻辑回归 | ❌ 没必要 | 天然可解释，不需要 SHAP |

SHAP 目前在风控领域最成熟的用法就是**配合树模型做异步拒因解释**。如果你用 LightGBM 做评分卡，SHAP 是标配。如果你用神经网络，先考虑要不要上——不是说不能用，而是性价比要掂量。

---

## 实操清单

每次上线一个新评分模型时，SHAP 分析作为上线前的"最后一道检查"：

1. **全局 summary plot**：看 Top 20 特征的 SHAP 分布是否合理、方向是否符合业务直觉
2. **交互效应 top 5 pairs**：找出交互最强的 5 对特征组合，人工确认逻辑成立
3. **随机抽 20 个被拒样本**：逐个看 SHAP 解释，询问策略同学"这个拒因你能给用户解释清楚吗"
4. **baseline 对齐**：确认线上客群的 expected_value 与模型训练时的基线差距在 5% 以内

第 3 步是最不可跳过的。如果你的模型拒了一个月入 3 万、信用记录良好的人，SHAP 给出的理由是"手机号归属地风险高"——可能模型没做错，但你需要确认这个理由在合规层面站得住脚。

---

*下一篇预告：特征工程——风控领域时间窗口特征设计，讲讲怎么把"最近 7 天""近 30 天""近 90 天"的特征体系搭好。*
