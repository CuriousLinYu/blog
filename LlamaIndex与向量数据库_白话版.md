# LlamaIndex 与向量数据库 —— 洗衣机没接水管，洗不了几件衣服

> 用最直白的方式讲清楚：为什么 LlamaIndex 没挂向量数据库等于武将没马。

---

## 一、LlamaIndex 到底是什么？

打个比方：**LlamaIndex 是一台洗衣机**。

| 洗衣机 | LlamaIndex |
|--------|------------|
| 把脏衣服洗干净 | 把你混乱的私有数据（PDF、数据库、代码、文档）变成 LLM 能用的"干净片段" |
| 洗→甩→烘干 流水线 | 加载→切片→向量化→检索→拼 prompt 流水线 |
| 洗完给穿 | 检索完塞给 LLM 做回答 |

整条链路：

```
你的私有数据（PDF/数据库/代码/文档）
        ↓ LlamaIndex 处理
精准的相关片段（3~5段最匹配的内容）
        ↓ 塞进 prompt
通用 LLM 变成了"懂你业务的专家"
```

说白了：LLM 的"聪明脑子"没变，LlamaIndex 只是帮它**开卷考试**——考前把答案翻到对的那一页递给它。没有 LlamaIndex，LLM 就是闭卷考你公司的内部知识，必挂。

---

## 二、向量数据库 = 洗衣机的水桶

- **没挂向量数据库** = 洗衣机自带一个矿泉水瓶当水箱 → 每次只能洗一条手帕
- **挂了向量数据库** = 洗衣机接上了自来水管 → 全家衣服随便洗

**向量数据库决定的是"容量"**：
- 内置小桶（内存 VectorStoreIndex）：几千条向量，程序一关全丢
- 外接大桶（Chroma / Pinecone / Milvus / Weaviate）：几亿条向量，持久化存储

---

## 三、有没有向量数据库，差距有多大？

| 数据规模 | 纯内存 | 挂向量数据库 | 评价 |
|---------|--------|------------|------|
| 几十页 PDF / 几个文档 | ✅ 轻松 | ✅ 轻松 | 两个方案都行 |
| 几百页～几千页 | ⚠️ 勉强 | ✅ 轻松 | 内存开始吃力，启动慢，占几个 G RAM |
| 几万页 / 整个知识库 | ❌ 撑不住 | ✅ 轻松 | 内存爆了，检索慢到不能用 |

**两个核心瓶颈**（纯内存方案绕不开的死穴）：

### 瓶颈 1：容量
内存是有限的。一台机器 16G/32G 内存，塞不了几百万条高维向量。向量数据库可以存硬盘上，用索引加速，几亿条照样秒检索。

### 瓶颈 2：持久化
程序一重启，内存里的向量全丢了。每次重启都要：
1. 重新把所有文档跑一遍切片
2. 重新把所有切片跑一遍 embedding（调 OpenAI API 要花钱的）
3. 重新把向量算出来存内存

→ **又慢又费钱**，生产环境根本扛不住。

---

## 四、LlamaIndex 用什么写的？架构什么样？

### 开发语言

**主版本：Python**。目前最成熟、生态最全的是 Python 版，也是生产环境的主流选择。

还有一个 **LlamaIndex.TS（TypeScript 版）**，给 Node.js 前端全栈用的，但功能比 Python 版少一些。

说白了：**主力用 Python，副线用 TS**。

### 架构：四层管道 + 三大支撑

把 LlamaIndex 拆开看，核心是一条"四段流水线"：

```
数据源 → [①加载] → [②索引] → [③检索] → [④查询引擎] → 生成回答
                      ↑                    ↑
                   存储层              模型层 + 提示层
```

**四段核心流水线（谁在前谁在后）**：

| 阶段 | 干什么 | 洗衣机类比 | 核心类/概念 |
|------|--------|----------|------------|
| **① 加载 Loading** | 从 PDF/数据库/API 等各种地方把数据读进来，切成小块（Node） | 把脏衣服丢进洗衣机 | `SimpleDirectoryReader`、`IngestionPipeline` |
| **② 索引 Indexing** | 把切好的文本转成向量，建立检索结构 | 洗衣机开始洗（把衣服和水搅一起） | `VectorStoreIndex`、`KnowledgeGraphIndex` |
| **③ 检索 Retrieval** | 用户提问后，从索引里捞出最相关的片段 | 甩干——把水甩掉，只留衣服 | `Retriever`、`QueryEngine` |
| **④ 查询引擎 Querying** | 把检索到的片段 + 用户问题拼成 prompt，发给 LLM 生成回答 | 晾干叠好，交给穿衣服的人 | `ChatEngine`、`ResponseSynthesizer` |

**三大支撑层（不在主流水线上但缺了不行）**：

| 支撑层 | 干什么 |
|--------|--------|
| **模型层 Models** | 管 LLM 和 Embedding 模型的调用（OpenAI / Claude / 本地模型） |
| **存储层 Storing** | 管向量存在哪、原始文档存哪、索引元数据存哪 |
| **提示层 Prompts** | 管 prompt 模板怎么写、怎么拼 |

**高级扩展（进阶玩家的玩具）**：

- **Agents 代理**：让 AI 自己决定调用哪个工具、查哪个索引
- **Workflows 工作流**：编排多步骤复杂任务（先查 A、再调 B、最后汇总）
- **Observability 可观测**：监控每条流水线跑了多久、花了多少钱、召回率多少

### 一句话版架构

> LlamaIndex = 加载数据 → 建索引 → 检索 → 塞 LLM，四段管道。模型、存储、提示是三条辅路，Agent 和工作流是上层玩法。

---

## 五、什么时候必须挂数据库？

| 场景 | 要不要挂 | 理由 |
|------|---------|------|
| 学习 / Demo / 跑通流程 | 不用 | 几个文档，内存足够 |
| PoC 验证效果 | 可以不用 | 先用小数据集验证"这套方案到底行不行" |
| 生产上线 | **必须挂** | 数据多、要持久化、服务不能重启就丢数据 |

---

## 六、一句话总结

> LlamaIndex 没挂向量数据库 = 洗衣机接了个矿泉水瓶当水箱，能洗，但只能洗一条手帕。要洗全家衣服，得接自来水管。

**没有向量数据库，LlamaIndex 只能做 demo；有了向量数据库，LlamaIndex 才能真正干活。**

---

## 七、LLM + 向量数据库到底怎么协作的？

以一个具体例子串起来。假设你问 AI "公司今年的年假政策是什么？"。

### 第 0 步（一次性准备）：把公司文档喂进向量库

```
员工手册 PDF
    ↓ Embedding 模型（如 text-embedding-3-small）
每条文本 → 一串数字（向量），如：
  "年假每年 15 天"  →  [0.023, -0.451, 0.892, ...]（1536 个数字）
  "病假需医院证明"  →  [0.018, -0.382, -0.712, ...]
  "加班可调休"      →  [-0.104, 0.276, 0.554, ...]
    ↓ 存入
向量数据库（存着：原文 + 对应向量）
```

**这 1536 个数字是什么意思？** 说白了就是一串"语义坐标"——意思相近的句子，它们的向量在数学空间里也挨得近。

### 第 1 步：用户提问 → 问题也变成向量

```
用户问："公司今年的年假政策是什么？"
    ↓ 同一个 Embedding 模型
问题向量：[0.027, -0.448, 0.901, ...]
```

### 第 2 步：向量库做相似度搜索

向量库把问题向量和库里所有文档向量做余弦相似度计算：

```
问题向量 [0.027, -0.448, 0.901, ...]
    vs
  "年假每年 15 天"  → 相似度 0.97 ✅
  "带薪病假 5 天"   → 相似度 0.81 ✅
  "报销需部门审批"  → 相似度 0.12 ❌
```

返回 Top-K 最相似的原文片段。

### 第 3 步：原文塞进 prompt，交给 LLM

```markdown
[参考资料]
1. "年假每年 15 天，工龄满 10 年增加至 20 天"
2. "年假未休完可延至次年 3 月底"

[用户问题]
公司今年的年假政策是什么？

→ LLM 回答："年假每年 15 天，工龄满 10 年可增加至 20 天……"
```

### 三个角色各司其职

| 角色 | 干了什么 | 没干什么 |
|------|---------|---------|
| **Embedding 模型** | 把文本变成数字向量 | 不理解你在问什么 |
| **向量数据库** | 存向量、算相似度、返回原文 | 不理解文本含义，只做数学 |
| **LLM** | 拿着原文 + 问题，生成人类可读的回答 | 不搜索、不检索，只生成文字 |

**说白了：向量库是"图书管理员"（帮你找书），LLM 是"教授"（看书回答问题）。**

---

## 八、Embedding 模型在哪？它跟向量数据库、LLM 是什么关系？

**Embedding 模型是一个独立的第三方**，不在向量数据库里，也不在 LLM 里。三个是分开的独立组件。

### 三者的物理位置

```
你的应用代码（调度员）
    │
    ├── ① 调 Embedding API ──→  OpenAI text-embedding-3-small（独立服务）
    │    "年假15天" → [0.023, -0.451, ...]
    │
    ├── ② 存/查向量 ──→  向量数据库（Milvus / Pinecone / Chroma，独立服务）
    │
    └── ③ 调 LLM ──→  GPT-4 / Claude（独立服务）
```

**每个都在不同进程/不同服务器上，通过 HTTP/gRPC 相互调用，不是谁在谁里面。**

### "向量数据库内置 embedding"到底怎么做到的？

以 **Weaviate** 为例：你在建 Collection 时配置好"用 `text-embedding-3-large`，这是 API Key"：

```python
client.collections.create(
    "知识库",
    vector_config=[Configure.Vectors.text2vec_openai(
        source_properties=["title"],
        model="text-embedding-3-large",
        dimensions=1024
    )]
)
```

之后每次你往 Weaviate 写数据或做查询，**Weaviate 内部自动帮你发 HTTP 请求**：

```
你写入 {"title": "年假15天"}
    │
    ▼
Weaviate 内部:
  ① 提取 title 字段值
  ② POST https://api.openai.com/v1/embeddings
     Body: {"input": "年假15天", "model": "text-embedding-3-large"}
     Header: Authorization: Bearer sk-xxx（你的 key）
  ③ OpenAI 返回向量 [0.023, -0.451, ...]
  ④ Weaviate 存入自己的向量索引
```

**它只是帮你省了"手动调 API → 手动存向量"这两步代码，网络请求一个没少。**

不同向量数据库对 embedding 的支持程度不同：

| 向量数据库 | embedding 能力 |
|-----------|--------------|
| **Weaviate** | 内置，配置后自动调 OpenAI / Cohere / HuggingFace |
| **Pinecone** | 内置集成，类似 Weaviate |
| **Chroma** | 内置，默认本地跑 `all-MiniLM-L6-v2`，也可配 OpenAI |
| **Milvus** | **没有内置**，必须自己调 embedding 模型拿到向量后再存 |

---

## 九、为什么叫"Embedding 模型"而不是"用来 embedding 的 LLM"？

因为它们**根本不是一种东西**。名字不同是因为训练目标、架构、输入输出全不同：

| | Embedding 模型 | LLM（大语言模型） |
|---|---|---|
| **输入** | 一段文本 | 一段 prompt（问题 + 上下文） |
| **输出** | 固定长度浮点数数组，如 `[0.023, -0.451, ...]`（1536 个数字） | 可变长度自然语言文本 |
| **核心任务** | "这段话长什么样？"——把语义映射成数学坐标 | "接下来该说什么？"——一个字一个字往下接 |
| **训练方式** | **对比学习**：让相似句子的向量靠近，无关的远离 | **自回归**：给前面的 token，猜下一个 |
| **架构** | **双向编码器**（BERT 风格）——同时看到整句上下文 | **单向解码器**（GPT 风格）——只能看到前面的词 |
| **参数规模** | 小（2000 万 ~ 几亿），CPU 都能跑 | 大（几十亿 ~ 万亿），必须 GPU |
| **速度/成本** | 极快，每秒几万条，几分钱 | 慢，每次生成要秒级，贵得多 |

### 打个比方

> **Embedding 模型 = 尺子**——量两句话的语义距离，精确到小数点，又快又便宜。
> **LLM = 作家**——看完资料写文章，贵但产出好。
>
> 你不会拿尺子写文章，也不会拿作家来量距离。

### 能不能用 LLM 做 embedding？

技术上可以，但**必须二次训练**。比如拿 Mistral-7B（一个 LLM），喂对比学习训练让它学会把相似语义拉近。训练完它就变成了 embedding 模型，也失去了一部分生成能力。**决定模型身份的是它被训练来干什么，不是它的底层是 encoder 还是 decoder。**

---

## 十、市面上有哪些经典 Embedding 模型？

### 商业 API 类（闭源，走网络调，按量付费）

| 模型 | 公司 | 维度 | 一句话 |
|------|------|------|--------|
| `text-embedding-3-small` | **OpenAI** | 1536 | 性价比之王，$0.02/百万 token |
| `text-embedding-3-large` | **OpenAI** | 3072 | 高精度，生态最成熟 |
| `embed-v4` | **Cohere** | 1536 | 多语言最强（100+语言），上下文 128K |
| `voyage-4` | **Voyage AI** | 1024 | 价格-精度比最优，$0.06/百万 |
| `voyage-4-large` | **Voyage AI** | 1024 | 检索精度天花板 |
| `voyage-4-lite` | **Voyage AI** | 1024 | 最便宜商业方案，$0.02/百万 |
| `gemini-embedding-001` | **Google** | 3072 | Google 生态深度集成 |
| `Titan Text Embeddings V2` | **Amazon（Bedrock）** | 1024 | AWS 原生，VPC 内合规 |

> Voyage AI 是一家专门做 embedding 的创业公司，不做 LLM，只做 embedding。精度目前第一梯队。

### 开源自部署类（自己机器跑，数据不出门）

| 模型 | 机构 | 参数量 | 维度 | 一句话 |
|------|------|--------|------|--------|
| `BGE-M3` | **BAAI（智源研究院）** | 5.68 亿 | 1024 | **中文开源标杆**，支持 100+ 语言，三种检索方式合一 |
| `BGE-large-zh-v1.5` | **BAAI（智源）** | 3.26 亿 | 1024 | 纯中文场景最成熟 |
| `GTE-Qwen2-7B-instruct` | **阿里（通义）** | 70 亿 | 3584 | 专业场景（法律/医疗）精度最高 |
| `Qwen3-Embedding-4B` | **阿里（通义）** | 40 亿 | 2560 | 通义最新 embedding 模型 |
| `moka-ai/m3e-base` | **Moka（墨奇）** | 1.1 亿 | 768 | 中文友好，仅 500MB，小显存可跑 |
| `jina-embeddings-v3` | **Jina AI** | 5.72 亿 | 1024（可调） | Matryoshka 降维，一个模型出多种维度 |
| `all-MiniLM-L6-v2` | **Microsoft** | 2200 万 | 384 | 入门首选，CPU 都能跑，每秒几万条 |

### 怎么选？三问就够了

1. **数据能出门吗？** 能 → API（OpenAI/Cohere/Voyage）；不能 → 自部署（BGE-M3/GTE-Qwen2）
2. **主要什么语言？** 主中文 → BGE-M3 / BGE-large-zh-v1.5；多语言 → Cohere embed-v4 / BGE-M3
3. **预算紧不紧？** 紧 → `voyage-4-lite`（API）/ `all-MiniLM-L6-v2`（本地）；不紧 → `voyage-4-large` / `GTE-Qwen2-7B`